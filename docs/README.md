# Documentation

This folder contains focused command documentation for the unified `esl-psc` CLI.

- `commands/pairs.md`: auto pair selection (`esl-psc pairs`)
- `commands/site-counter.md`: site counting (`esl-psc site-counter`)

General project docs, installation, and demos remain in the repository `README.md`.
